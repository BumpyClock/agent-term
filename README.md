Inspired by the agent-deck TUI (https://github.com/asheshgoplani/agent-deck).

Agent Term is now a pure Rust + GPUI app (Alacritty-backed terminal) for juggling multiple agent sessions across projects in one place.

## Development

```sh
./scripts/init-submodules.sh
CARGO_BUILD_JOBS=1 cargo run
```
